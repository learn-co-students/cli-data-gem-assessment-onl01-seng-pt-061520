require 'spec_helper'

describe mlb_top_stories do
  it 'has a version number' do
    expect(mlb_top_stories::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end